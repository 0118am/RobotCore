[Unit]
# A RobotCore restart is a complete camera-stack restart.  Ordering from
# robotcore.service ensures the ROS/ZED client stops before Argus and that a
# fresh Argus daemon is running before the new graph starts.
PartOf=robotcore.service
Before=robotcore.service
