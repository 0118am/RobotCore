[Unit]
# Restart the ZED X hardware daemon exactly once with every RobotCore restart.
# zed_x_daemon.service already starts after nvargus-daemon.service.
PartOf=robotcore.service
Before=robotcore.service
