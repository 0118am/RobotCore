"""Runtime nodes for EUPSystemInfraPack.

This package owns system-level orchestration state rather than backend-specific
logic, so it can run against both MuJoCo and the future edge hardware path.
"""
